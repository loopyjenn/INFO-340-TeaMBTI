import { KbdTheme } from './KbdTheme';
import { ChipProps } from '@/elements/Chip';
import { FC } from 'react';

export interface KbdProps extends Omit<ChipProps, 'children' | 'theme'> {
    /**
     * Keycode for the Kbd.
     */
    keycode: string;
    /**
     * Theme for the Kbd.
     */
    theme?: KbdTheme;
}
export declare const Kbd: FC<KbdProps>;
