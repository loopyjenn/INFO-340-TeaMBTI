import { InputProps, InputRef } from '@/form/Input/Input';
import { FC } from 'react';

export interface DebouncedInputProps extends InputProps {
    /**
     * The debounce time in milliseconds. Defaults to 100.
     */
    debounce?: number;
}
export declare const DebouncedInput: FC<DebouncedInputProps & InputRef>;
